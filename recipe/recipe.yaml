context:
  name: apache-airflow-providers-amazon
  version: "9.36.0"
  latest_airflow: 3.1.8

recipe:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/apache_airflow_providers_amazon-${{ version }}.tar.gz
  sha256: 0151d8542d66be83639063bd74376c4a49ecba17e553726e585bb5cb2e5bdc6d

build:
  noarch: python
  number: 0

outputs:
  - package:
      name: ${{ name }}
    build:
      noarch: python
      script: python -m pip install . -vv --no-deps --no-build-isolation
    requirements:
      host:
        - python ${{ python_min }}.*
        - pip
        - flit-core ==3.12.0
      run:
        - python >=${{ python_min }}
        - apache-airflow >=2.11.0
        - apache-airflow-providers-common-compat >=1.13.0
        - apache-airflow-providers-common-sql >=1.32.0
        - apache-airflow-providers-http
        - boto3 >=1.41.0
        - botocore >=1.41.0
        - inflection >=0.5.1
        - watchtower >=3.3.1,<4
        - jsonpath-ng >=1.5.3
        - redshift_connector >=2.1.3
        - asgiref >=2.3.0
        - pyathena >=3.10.0
        - jmespath >=0.7.0
        - sagemaker-studio >=1.0.9
        # more restrictive for python >=3.13
        - pydynamodb >=0.7.5
        # more restrictive for python >=3.13
        - sqlean.py >=3.47.0
        - marshmallow >=3

        # temproary constraints to avoid pip check problems
        - pyiceberg >=0.8.1
        - pynacl <1.6.1
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-aiobotocore
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - aiobotocore >=2.26.0
        # start aiobotocore[boto3]
        # end aiobotocore[boto3]
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-apache-hive
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-apache-hive
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-cncf-kubernetes
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-cncf-kubernetes >=7.2.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-common-messaging
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-common-messaging >=2.0.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-exasol
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-exasol
        # temporary constraint because of bad pyexasol dependencies
        - packaging >=24.1.0,<26.0.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-fab
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-fab >=2.2.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-ftp
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-ftp
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-google
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-google
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-imap
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-imap
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-microsoft-azure
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-microsoft-azure
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-mongo
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-mongo
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-openlineage
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-openlineage >=2.3.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-pandas
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        # conditional for python <3.13
        - pandas >=2.1.2
        # conditional for python >=3.13
        - pandas >=2.2.3
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-python3-saml
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        # conditional for python <3.13
        - python3-saml >=1.16.0
        - xmlsec >=1.3.14
        - lxml >=6.0.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-s3fs
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - s3fs >=2023.10.0
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-salesforce
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-salesforce
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-ssh
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-ssh
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  - package:
      name: ${{ name }}-with-standard
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage(name, exact=True) }}
        - apache-airflow-providers-standard
    tests:
      - requirements:
          run:
            - python ${{ python_min }}.*
            - pip
            - apache-airflow ${{ latest_airflow }}.*
        script:
          - python -c "import airflow.providers.amazon"
          - pip check

  # skipping output sqlalchemy

about:
  homepage: https://github.com/apache/airflow/
  summary: Provider package apache-airflow-providers-amazon for Apache Airflow
  license: Apache-2.0
  license_file:
    - LICENSE
    - NOTICE
  documentation: https://airflow.apache.org/docs/apache-airflow-providers-amazon/stable/index.html
  repository: https://github.com/apache/airflow/

extra:
  recipe-maintainers:
    - xylar
